# Database Configuration (Shared with Django Backend)
db.url=jdbc:postgresql://localhost:5432/cinema
db.user=trantuanduong
db.password=
db.poolSize=10
db.schema=public

# Application Settings
app.name=Cinema Ticket Management System
app.version=1.0.0
booking.expiry.minutes=10
